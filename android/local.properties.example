# Copy to local.properties and fill in. Do not commit local.properties.
# Flutter SDK path (set by 'flutter config' or 'flutter run')
# sdk.dir=/path/to/Android/sdk
# flutter.sdk=/path/to/flutter

# Google Maps API key (required for map and Places features)
GOOGLE_MAPS_API_KEY=

# Firebase Storage token for splash logo (optional)
SPLASH_LOGO_TOKEN=
